import Image from "next/image";
import Link from "next/link";
import { ArrowUpRight, BedDouble, Bath, Maximize2 } from "lucide-react";
import type { Property } from "@/lib/types";
import { propertyPrice } from "@/lib/format";
export default function PropertyCard({ property, priority = false }: { property: Property; priority?: boolean }) { return <article className="group overflow-hidden bg-white shadow-card"><Link href={`/properties/${property.slug}`} className="block"><div className="relative aspect-[1.18] overflow-hidden"><Image src={property.images[0]} alt={property.title} fill priority={priority} sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw" className="object-cover transition duration-700 group-hover:scale-105" /><span className="absolute left-4 top-4 bg-navy px-3 py-1.5 text-[.65rem] font-bold uppercase tracking-wider text-cream">{property.status}</span><span className="absolute right-4 top-4 grid h-9 w-9 place-items-center rounded-full bg-cream text-navy transition group-hover:bg-gold"><ArrowUpRight size={17} /></span></div><div className="p-5"><p className="eyebrow">{property.locality} · {property.city}</p><h3 className="display mt-2 text-3xl leading-none">{property.title}</h3><p className="mt-3 font-semibold text-navy">{propertyPrice(property)}</p><div className="mt-5 flex gap-4 border-t border-navy/10 pt-4 text-xs text-navy/65"><span className="inline-flex items-center gap-1"><BedDouble size={14}/>{property.bedrooms || "—"} Beds</span><span className="inline-flex items-center gap-1"><Bath size={14}/>{property.bathrooms || "—"} Baths</span><span className="inline-flex items-center gap-1"><Maximize2 size={13}/>{property.area} {property.areaUnit}</span></div></div></Link></article>; }
